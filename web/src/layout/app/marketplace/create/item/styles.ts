import { styled } from 'stitches.config'

export const Container = styled('div', {
  display: 'flex',
  alignItems: 'center',
  minHeight: '100vh'
})

export const Form = styled('form', {
  width: '100%'
})
