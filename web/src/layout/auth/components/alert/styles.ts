import { styled } from 'stitches.config'

export const Container = styled('div', {
  width: '100%',
  background: '$tertiary',
  position: 'fixed',
  bottom: 0,
  left: 0,
  zIndex: 22,
  padding: '1rem 2rem'
})